  ┌──────────────┐              ┌──────────────┐        ┌──────────────┐
  │ profile.yml  │─── parse ───▶│   Profile    │        │    Harness   │
  └──────────────┘              └──────┬───────┘        └──────┬───────┘
                                       │  1                 1  │
                                       └──────────┬────────────┘
                                                  │
  ┌──────────────┐                        ┌───────┴────────┐
  │ sandbox.yml  │──────── parse ────────▶│  SandboxSpec   │   binds 1 harness + 1 profile
  └──────────────┘                        └───────┬────────┘
                                                  │ instantiate
                                                  ▼
                                          ┌────────────────┐        ┌──────────────────┐
                                          │    Sandbox     │───────▶│    container     │
                                          └───────┬────────┘        │  sleep infinity  │
                                                  │                 └──────────────────┘
                                                  │ API.Acquire() ───────│─── (auto) docker build · docker run
                                                  ▼                      │
                                          ┌────────────────┐             │
                                          │     Lease      │             │
                                          │  Extend()      │             │
                                          │  Run() ────────│─────────────┴──▶ docker exec  ──▶ RunResult
                                          │  Release()     │
                                          └────────────────┘